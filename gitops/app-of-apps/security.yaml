apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: security
  namespace: argocd

spec:
  project: security
  source:
    repoURL: https://github.com/hieu190400/gitops-homelab
    targetRevision: main
    path: gitops/applications/security 

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true 