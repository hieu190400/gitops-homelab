apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: otel-operator
  namespace: argocd

spec:
  project: observability
  
  source:
    repoURL: https://github.com/hieu190400/gitops-homelab
    targetRevision: main
    path: platform/observability/otel-operator
    helm:
      releaseName: otel-operator
      

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true

