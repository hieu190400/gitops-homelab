opentelemetry-operator:
  manager:
    resources:
      requests:
        cpu: 50m
        memory: 128Mi

      limits:
        cpu: 200m
        memory: 256Mi

  admissionWebhooks:
    certManager:
      enabled: false