{{- if .Values.route.prometheus.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: prometheus
  namespace: {{ .Release.Namespace }}
spec:
  parentRefs:
    {{- toYaml .Values.gateway | nindent 4 }}

  hostnames:
    - {{ .Values.route.prometheus.host | quote }}

  rules:
    - backendRefs:
        - group: ''	
          kind: Service
          name: {{ printf "%s-kube-prometheus-st-prometheus" .Release.Name }}
          port: 9090
          weight: 1	
      matches:	
        - path:	
            type: PathPrefix	
            value: /
{{- end }}