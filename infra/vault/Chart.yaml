apiVersion: v2

name: vault

description: A Helm chart for deploying Vault resources

type: application

version: 0.1.0

appVersion: "1.0"

dependencies:
  - name: vault
    repository: https://helm.releases.hashicorp.com
    version: 0.33.0