opentelemetry-collector:
  mode: daemonset

  presets:
    logsCollection:
      enabled: true

    kubernetesAttributes:
      enabled: true
  
  resources:
    limits:
      cpu: 250m
      memory: 512Mi