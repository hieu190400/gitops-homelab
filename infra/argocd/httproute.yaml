apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: argocd
  namespace: argocd
spec:
  parentRefs:
    - name: web
      namespace: infra
      sectionName: web

  hostnames:
    - argocd.lab.local

  rules:
    - backendRefs:
        - name: argocd-server
          port: 80