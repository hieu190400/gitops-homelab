{{- if .Values.longhorn.route.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: longhorn
  namespace: {{ .Release.Namespace }}
spec:
  parentRefs:
    {{- toYaml .Values.gateway | nindent 4 }}

  hostnames:
    - {{ .Values.longhorn.route.host | quote }}

  rules:
    - backendRefs:
        - group: ''	
          kind: Service
          name: {{ printf "%s-longhorn-ui" .Release.Name }}
          port: 8000
          weight: 1	
      matches:	
        - path:	
            type: PathPrefix	
            value: /
{{- end }}