apiVersion: v2

name: longhorn

description: A Helm chart for deploying Longhorn resources

type: application

version: 0.1.0

appVersion: "1.0"

dependencies:
  - name: longhorn
    repository: https://charts.longhorn.io
    version: 1.12.0